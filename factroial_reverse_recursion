#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int n;
int fact(int i){
    if(i<=1)
        return 1;
    else
        return i*fact(i-1);
}


int main() {
    cin>>n;
   cout<<fact(n)<<" ";
    return 0;
}
